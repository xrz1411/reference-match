# 参考仿色 LUT 工具：已批准视觉规范

## 视觉定位

- 面向桌面端调色工作流的深色本地工具界面：克制、精确、接近专业后期软件，而不是消费级滤镜应用。
- 强调“参考图、视频静帧、匹配预览”的直接对照；风格分析与交付控制是辅助信息，不抢占画面。
- 只描述整体色彩和光影的匹配结果，避免将界面表述为局部或对象级调色。

## 色彩与层级

- 基底使用近黑绿 `#0B0E0D`，面板使用深绿灰 `#111614` / `#151B18`，通过低对比边框 `#29312D` 划分区域。
- 主文字使用暖白 `#F1F3EC`，辅助文字使用灰绿 `#A4AAA2`；信息状态使用低饱和绿点。
- 琥珀黄 `#E9C363` 仅用于可执行的主操作、选中状态与小标题，避免大面积铺色。
- RGB 示波器沿用红、绿、蓝的通道色；矢量示波器以低亮度绿色密度点呈现。

## 布局与密度

- 顶部依次展示三张等权画面：参考图、视频静帧、匹配预览。三者使用相同卡片尺寸，图片完整包含于画框内，不裁切。
- 主色调提取与图像示波器位于同一分析面板；左侧为色板，右侧为横排 RGB 直方图与矢量示波器。
- 主色板固定为十列，按感知明度由暗到亮排列；每个色块必须保持正方形，并显示对应色值。
- 交付控制集中在分析面板下方的一行分组区：强度、区域权重、保护、色彩空间和导出操作并列显示，减少上下滚动。
- 常规桌面宽度下维持紧凑、可扫描的横向布局；窄窗口允许降低间距和缩小预览高度，但不改变色板正方形约束。

## 表面与控件

- 面板采用 16px 圆角、细描边和极轻的深色渐变，不使用明显投影或玻璃拟态。
- 主要按钮用琥珀色实底；次级导出按钮使用深色底、浅边框。选中与悬停仅通过边框、文字或轻微亮度差提示。
- 滑杆为细轨道配圆形滑块；开关为低饱和绿色开启状态，避免引入额外强调色。

## 数据呈现与状态

- 色板来自参考图提取结果，展示顺序服务于视觉连续性，不代表覆盖面积排名。
- 直方图保留真实像素统计，显示时可平滑和降采样；矢量示波器显示真实 Cb/Cr 密度。
- 在无可读取像素时，保留简短、明确的降级说明，不以伪造波形代替真实数据。
- 所有交互控件应保留清晰文本标签与可见选中状态；Canvas 图表应提供可访问名称。
